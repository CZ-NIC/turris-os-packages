diff -ur a/lib/readline/readline.c b/lib/readline/readline.c
--- a/lib/readline/readline.c	2014-09-25 13:08:30.526480374 +0200
+++ b/lib/readline/readline.c	2014-09-25 13:08:30.549813707 +0200
@@ -965,7 +965,7 @@
 #if defined (VI_MODE)
   if (rl_editing_mode == vi_mode && _rl_keymap == vi_movement_keymap &&
       key != ANYOTHERKEY &&
-      rl_key_sequence_length == 1 &&	/* XXX */
+      _rl_dispatching_keymap == vi_movement_keymap &&
       _rl_vi_textmod_command (key))
     _rl_vi_set_last (key, rl_numeric_arg, rl_arg_sign);
 #endif
Only in b/lib/readline: readline.c.orig
diff -ur a/patchlevel.h b/patchlevel.h
--- a/patchlevel.h	2014-09-25 13:08:30.509813706 +0200
+++ b/patchlevel.h	2014-09-25 13:08:30.549813707 +0200
@@ -25,6 +25,6 @@
    regexp `^#define[ 	]*PATCHLEVEL', since that's what support/mkversion.sh
    looks for to find the patch level (for the sccs version string). */
 
-#define PATCHLEVEL 3
+#define PATCHLEVEL 4
 
 #endif /* _PATCHLEVEL_H_ */
