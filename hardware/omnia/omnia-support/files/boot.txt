setenv bootargs 'earlyprintk console=ttyS0,115200 rootfstype=btrfs rootwait root=/dev/mmcblk0p1 rootflags=subvol=@,commit=5 rw ${contract}'

if gpio input gpio@71_4; then
    setenv wan sfp
else
    setenv wan phy
fi

load mmc 0 ${kernel_addr_r} /@/boot/zImage @
load mmc 0 ${fdt_addr_r} /@/boot/armada-385-turris-omnia-$wan.dtb @
bootz ${kernel_addr_r} - ${fdt_addr_r}
