MD5=$(
(
	route -n
	route -n -A inet6
) | md5sum | cut -f1 -d\ )
PREVIOUS=$(cat /tmp/route.md5 || true)
if [ "$MD5" != "$PREVIOUS" ] ; then
	/etc/init.d/ucollect reload
	echo "$MD5" >/tmp/route.md5
fi
