- eve-log:
      enabled: yes
      filetype: unix_dgram
      filename: /var/run/suricata_monitor.sock
      types:
        - alert
        - flow
        - dns:
            query: no
        - tls:
            extended: yes
        - http:
            extended: yes

